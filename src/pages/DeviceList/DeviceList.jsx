//CSS
import styles from './DeviceList.module.css'

const DeviceList = () => {
  return (  
    <main className={styles.container}>
      <h1>Device List</h1>
    </main>
  )
}

export default DeviceList